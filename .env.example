# Copia este archivo como .env y rellena los valores reales.
# El .env contiene secretos y NUNCA se sube a GitHub (ver .gitignore).
DB_HOST=localhost
DB_NAME=fulgor_medieval
DB_USER=fulgor_user
DB_PASS=cambia-esta-contrasena
